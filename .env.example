# ========================================
# AGROMART - ENVIRONMENT CONFIGURATION
# ========================================
# Copy this file to .env and fill in your actual values
# DO NOT commit the .env file to version control!

# ========================================
# Server Configuration
# ========================================
NODE_ENV=production
PORT=3000

# ========================================
# Google Sheets Configuration
# ========================================
# Generate these from Google Cloud Console:
# 1. Create a Google Cloud Project
# 2. Enable Google Sheets API
# 3. Create a Service Account and download JSON key
# 4. Share your Google Sheet with the service account email

# Your Google Sheet ID (from the URL)
GOOGLE_SHEET_ID=your_sheet_id_here

# Service Account Email (from the JSON key file)
GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com

# Private Key (from the JSON key file - be careful with newlines!)
GOOGLE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n

# ========================================
# Admin Configuration
# ========================================
# Change these to secure values!
# Use strong, random passwords in production
ADMIN_PASSWORD=change_me_to_a_strong_password
ADMIN_PATH=change_me_to_random_path

# ========================================
# CORS Configuration
# ========================================
# Set this to your frontend URL (for Vercel, use your Vercel project URL)
# Example: https://agromart.vercel.app
CLIENT_URL=http://localhost:3000

# For Vercel deployments, set this env var automatically
# VERCEL_URL=your-project.vercel.app

# ========================================
# Security Notes
# ========================================
# - NEVER commit this file to version control
# - Use strong, random passwords (min 32 characters for production)
# - Rotate admin password regularly
# - Keep the private key secure and never expose it
# - In production, use Vercel's Environment Variables feature
# - Enable HTTPS only (Vercel handles this automatically)
